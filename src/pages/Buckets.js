import React from "react";
import BucketItems from "../components/BucketItems";

const Buckets = () => {
  return (
    <React.Fragment>
      <BucketItems />
    </React.Fragment>
  );
};

export default Buckets;
